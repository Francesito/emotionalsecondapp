name: emotionaltwo
description: "InduStat - Proyecto integrador de estadística aplicada a la industria."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  http: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.13.1

flutter_launcher_icons:
  android: true
  ios: true
  image_path: assets/icon/industat.png
  adaptive_icon_background: "#121330"
  adaptive_icon_foreground: assets/icon/industat.png

flutter:
  uses-material-design: true
